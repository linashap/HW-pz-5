'''В заданной строке расположить в обратном порядке все слова. Разделителями
слов считаются пробелы'''
pred=str(input('введите строку'))
pred_list = pred.split(' ') #разделяет по знаку
pred_list.reverse() #меняет местами
result = ' '.join(pred_list) #объединяет по символу

print(result)